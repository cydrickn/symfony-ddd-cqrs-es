<?php

declare(strict_types = 1);

namespace App\Command\{{ domain }};

use Cydrickn\DDD\Common\Command\AbstractCommandHandler;
use {{ namespace }}\Repository\{{ domain }}RepositoryInterface;
use Symfony\Component\Messenger\Handler\MessageSubscriberInterface;

/**
 * Description of {{ domain }}Handler
 *
 * @author {{ author.name }} <{{ author.email }}>
 */
class {{ domain }}Handler extends AbstractCommandHandler implements MessageSubscriberInterface
{
    /**
     * @var {{ domain }}RepositoryInterface
     */
    private ${{ (domain ~ 'Repository')|camel_case  }};


    public static function getHandledMessages(): iterable
    {
    }

    public function __construct({{ domain }}RepositoryInterface ${{ (domain ~ 'Repository')|camel_case  }}) {
        $this->{{ (domain ~ 'Repository')|camel_case  }} = ${{ (domain ~ 'Repository')|camel_case  }};
    }
}

